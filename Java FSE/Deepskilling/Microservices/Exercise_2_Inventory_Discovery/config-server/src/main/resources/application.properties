server.port=8888
spring.application.name=config-server

# Use native local directory rather than Git repository
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=classpath:/config
